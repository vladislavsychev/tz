<div style="margin-bottom: 30px"> 
     <span style="color: #d96; font-size: 1.4em">
       <%= contract.t_car.upcase %>
      на <%= contract.lease_time %> часа/ов
     </span> 
    <br>
     <span style="color: #696; font-size: 1.2em"> 
       <%= I18n.l contract.date_rent, format: :long %> 
       <%= contract.time_rent %>
       для <%= contract.contractor_name %>
     </span> 
     <% if !current_user.nil? && current_user.admin? %>
      <br/> 
       < <%= contract.contractor_email %> >
       <%= contract.contractor_mphone %> 
       [ <%= link_to "delete", contract, method: :delete, data: { confirm: "You sure?" } %>
       |   <%= link_to "edit", edit_contract_path(contract.id) %>
       ]<br/>
     <% end %>

  <span class="content">   
     <%= link_to wrap(contract.body_contract), contract %>
  </span>
  <small>
   Предложений 
      <%= contract.offers.count %>
  </small>
</div>




