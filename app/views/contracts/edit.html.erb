<% provide(:title, "Редактировать предзаказ") %>
<h1>Редактировать предзаказ.</h1>
<div class="row">
  <div class="span10 offset1">
    <%= form_for(@contract) do |f| %>
     <div class='span3'>
      <%= render 'valid_field' %>
     </div>
      <%= render 'contract_field', f: f %>

    <% end %>

  </div>
</div>
